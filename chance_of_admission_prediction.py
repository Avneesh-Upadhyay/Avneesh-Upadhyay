# -*- coding: utf-8 -*-
"""Chance of Admission Prediction.ipynb

Automatically generated by Colaboratory.

Original file is located at
    https://colab.research.google.com/drive/1YVD36GNEMpc9jLY9941EMr4FGe1JIdnq

**Chance of Admission Prediction**

**objective - The aim is to assist students in gaining admission to the best university while also providing insights into the likelihood of admission to other universities based on their scores.**

**Data Source**  - https://github.com/YBI-Foundation/Dataset/blob/main/Admission%20Chance.csv

**Import Library**
"""

import pandas as pd

"""**Import Data**"""

admission = pd.read_csv("https://github.com/YBI-Foundation/Dataset/raw/main/Admission%20Chance.csv")

admission.head()

admission.info()

"""**Describe Data**"""

admission.describe()

"""**Define Target Variable (y) and Feature Variables (X)**"""

admission.columns

y = admission['Chance of Admit ']

X = admission.drop(['Serial No','Chance of Admit '],axis=1)

"""
**Train Test Split**"""

from sklearn.model_selection import train_test_split
X_train, X_test, y_train, y_test = train_test_split(X,y, train_size=0.7, random_state=2529)

"""**check shape of train and test sample**"""

X_train.shape, X_test.shape, y_train.shape, y_test.shape

"""**Modeling**"""

from sklearn.linear_model import LinearRegression
model = LinearRegression()

"""**train or fit model**"""

model.fit(X_train,y_train)

model.intercept_

model.coef_

"""**predict model**"""

y_pred = model.predict(X_test)

y_pred

"""** model accuracy**"""

from sklearn.metrics import mean_absolute_error, mean_absolute_percentage_error, mean_squared_error

mean_absolute_error(y_test,y_pred)

mean_absolute_percentage_error(y_test,y_pred)

mean_squared_error(y_test,y_pred)